# api/conductor/urls.py
# Endpoints BFF (Backend For Frontend) específicos para los Conductores

from django.urls import path

urlpatterns = [
    # TODO: Migrar aquí vistas del conductor (Mis Rutas, Tableros, Paradas) en las próximas sesiones
    # path('rutas/', ConductorRutasVista.as_view(), name='conductor-rutas'),
]
