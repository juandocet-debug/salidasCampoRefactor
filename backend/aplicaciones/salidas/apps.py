from django.apps import AppConfig

class SalidasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'aplicaciones.salidas'
    label              = 'salidas'
    verbose_name       = 'Salidas'
