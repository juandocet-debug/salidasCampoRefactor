from django.apps import AppConfig

class AbordajeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'aplicaciones.abordaje'
    label              = 'abordaje'
    verbose_name       = 'Abordaje'
